// This script handles the flags that are used to determine what is happening in the game.
using UnityEngine;

namespace LunarflyArts
{
    public class GameplayFlagsManager : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
